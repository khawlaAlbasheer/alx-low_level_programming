#include <stdio.h>
/**
 * main - prints program name
 * @argc: argument counter
 * @argv: argoment vector
 * Return: 0 Always success
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
